TYPE=TRIGGERS
triggers='CREATE DEFINER=`root`@`%` TRIGGER `tr_Insert_Trk_Repair_Executor` AFTER INSERT ON `trk_repair_executors` FOR EACH ROW BEGIN\n                INSERT INTO `log_trk_repair_executors` (\n                    `id`,\n                    `trk_room_repair_id`,\n                    `executor_id`,\n                    `author_id`,\n                    `last_editor_id`,\n                    `destroyer_id`,\n                    `comment`,\n                    `sort_order`,\n                    `created_at`,\n                    `updated_at`,\n                    `deleted_at`\n                )\n                VALUES(\n                    NEW.id,\n                    NEW.trk_room_repair_id,\n                    NEW.executor_id,\n                    NEW.author_id,\n                    NEW.last_editor_id,\n                    NEW.destroyer_id,\n                    NEW.comment,\n                    NEW.sort_order,\n                    NEW.created_at,\n                    NEW.updated_at,\n                    NEW.deleted_at\n                );\n                END' 'CREATE DEFINER=`root`@`%` TRIGGER `tr_Update_Trk_Repair_Executor` AFTER UPDATE ON `trk_repair_executors` FOR EACH ROW BEGIN\n                INSERT INTO `log_trk_repair_executors` (\n                    `id`,\n                    `trk_room_repair_id`,\n                    `executor_id`,\n                    `author_id`,\n                    `last_editor_id`,\n                    `destroyer_id`,\n                    `comment`,\n                    `sort_order`,\n                    `created_at`,\n                    `updated_at`,\n                    `deleted_at`\n                )\n                VALUES(\n                    NEW.id,\n                    NEW.trk_room_repair_id,\n                    NEW.executor_id,\n                    NEW.author_id,\n                    NEW.last_editor_id,\n                    NEW.destroyer_id,\n                    NEW.comment,\n                    NEW.sort_order,\n                    NEW.created_at,\n                    NEW.updated_at,\n                    NEW.deleted_at\n                );\n            END'
sql_modes=524288 524288
definers='root@%' 'root@%'
client_cs_names='utf8mb4' 'utf8mb4'
connection_cl_names='utf8mb4_uca1400_ai_ci' 'utf8mb4_uca1400_ai_ci'
db_cl_names='utf8mb4_uca1400_ai_ci' 'utf8mb4_uca1400_ai_ci'
created=1745925863962402 1745925863980529
